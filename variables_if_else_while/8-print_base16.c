#include <stdio.h>
/**
 * main - Prints all the numbers of base 16 in lowercase
 * Return: 0 (Success)
 */

int main(void)
{
	int y;

	for (y = 0; y < 10 || y < '0'; y++)
	{
		if (i < 10)
			putchar(y + '0');
		else
			puchar(y);
	}
		putchar('\n');
		return (0);
}
