#include <stdio.h>
/**
 * main - C program that prints numbers of base 10 from 0
 * Return: 0 (Success)
 */

int main(void)
{
	int t;

	for (t = 0; t < 10; t++)
		putchar(t);
	putchar('\n');
	return (0);
}
