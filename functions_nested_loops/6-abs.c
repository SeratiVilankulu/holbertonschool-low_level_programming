#include "main.h"
#include <stdio.h>

/**
 * _abs - Function that computes the absolute value of an integer
 * @r: number to be checked
 * Return: 0 (Success)
 */

int _abs(int r)
{
	if (r >= 0)
	return (r);
	else
	return (-r);	
}
